[project]
name = "specodec-interop-python"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
  "specodec-runtime-python>=0.0.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"